# Documentation toolchain. Keep pinned so local builds and CI agree.
#
#   pip install -r docs/requirements.txt
#   mkdocs build --strict
mkdocs-material==9.7.7
mike==2.2.0
mkdocs-print-site-plugin==2.8
