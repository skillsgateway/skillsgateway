apiVersion: v1
kind: Service
metadata:
  name: {{ include "skills-gateway.fullname" . }}
  labels:
    {{- include "skills-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skills-gateway.selectorLabels" . | nindent 4 }}
