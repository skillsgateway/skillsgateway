{{- include "skills-gateway.storageGate" . -}}
{{- include "skills-gateway.replicaGate" . -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "skills-gateway.fullname" . }}
  labels:
    {{- include "skills-gateway.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "skills-gateway.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      {{- if .Values.config }}
      annotations:
        # Roll the pods when the layered configuration changes: a mounted
        # ConfigMap is refreshed in place, but the estate is reconciled at
        # startup, so without a restart the new configuration is never read.
        checksum/config: {{ toYaml .Values.config | sha256sum }}
      {{- end }}
      labels:
        {{- include "skills-gateway.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "skills-gateway.serviceAccountName" . }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: skills-gateway
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          {{- with .Values.securityContext }}
          securityContext:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          env:
            - name: SPRING_DATASOURCE_URL
              value: "jdbc:postgresql://{{ required "postgresql.host is required" .Values.postgresql.host }}:{{ .Values.postgresql.port }}/{{ .Values.postgresql.database }}"
            - name: SPRING_DATASOURCE_USERNAME
              value: {{ .Values.postgresql.username | quote }}
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ required "postgresql.existingSecret is required" .Values.postgresql.existingSecret }}
                  key: password
            - name: SGW_OIDC_CLIENT_ID
              value: {{ .Values.oidc.clientId | quote }}
            - name: SGW_OIDC_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ required "oidc.existingSecret is required" .Values.oidc.existingSecret }}
                  key: client-secret
            - name: SGW_OIDC_AUTHORIZATION_URI
              value: {{ .Values.oidc.authorizationUri | quote }}
            - name: SGW_OIDC_TOKEN_URI
              value: {{ .Values.oidc.tokenUri | quote }}
            - name: SGW_OIDC_JWK_SET_URI
              value: {{ .Values.oidc.jwkSetUri | quote }}
            - name: SGW_OIDC_USER_NAME_ATTRIBUTE
              value: {{ .Values.oidc.userNameAttribute | quote }}
            - name: SGW_OIDC_SCOPE
              value: {{ .Values.oidc.scope | quote }}
            {{- if .Values.oidc.issuer }}
            - name: SKILLSGATEWAY_OIDC_ISSUER
              value: {{ .Values.oidc.issuer | quote }}
            {{- end }}
            # The storage backend is named, never inferred -- here as in the
            # gateway, so what the values file says and what the pod does are
            # the same statement.
            - name: SKILLSGATEWAY_STORAGE_BACKEND
              value: {{ .Values.storage.backend | quote }}
            {{- if eq .Values.storage.backend "object-store" }}
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_BUCKET
              value: {{ .Values.storage.objectStore.bucket | quote }}
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_REGION
              value: {{ .Values.storage.objectStore.region | quote }}
            {{- if .Values.storage.objectStore.endpoint }}
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_ENDPOINT
              value: {{ .Values.storage.objectStore.endpoint | quote }}
            {{- end }}
            {{- if .Values.storage.objectStore.prefix }}
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_PREFIX
              value: {{ .Values.storage.objectStore.prefix | quote }}
            {{- end }}
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_CREDENTIALS_MODE
              value: {{ .Values.storage.objectStore.credentials.mode | quote }}
            {{- if eq .Values.storage.objectStore.credentials.mode "static" }}
            # Never from the values file: a ConfigMap is not a Secret, and an
            # access key is the one credential this deployment can still hold.
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_CREDENTIALS_ACCESSKEYID
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.storage.objectStore.credentials.existingSecret }}
                  key: access-key-id
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_CREDENTIALS_SECRETACCESSKEY
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.storage.objectStore.credentials.existingSecret }}
                  key: secret-access-key
            {{- end }}
            {{- if eq .Values.persistence.mode "none" }}
            # No volume: the pack cache is not authoritative, so it goes to the
            # writable temporary directory and is rebuilt on every start.
            - name: SKILLSGATEWAY_STORAGE_OBJECTSTORE_CACHE_DIR
              value: /tmp/pack-cache
            {{- end }}
            {{- end }}
            {{- if .Values.config }}
            # Layered on top of the application.yaml inside the image; the
            # image keeps the defaults, this file overrides them. `optional:`
            # keeps the container starting if the mount is ever absent.
            - name: SPRING_CONFIG_ADDITIONAL_LOCATION
              value: "optional:file:/etc/skills-gateway/"
            {{- end }}
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- with .Values.extraEnvFrom }}
          envFrom:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          # The exact /actuator/health path is the only unauthenticated
          # actuator endpoint; liveness/readiness subpaths would be
          # redirected to the IdP.
          volumeMounts:
            {{- if ne .Values.persistence.mode "none" }}
            - name: data
              mountPath: /data
            {{- end }}
            # The root filesystem is read-only, so the one path the runtime
            # still needs to write -- temporary files, including JGit's -- is
            # provided explicitly.
            - name: tmp
              mountPath: /tmp
            {{- if .Values.config }}
            - name: config
              mountPath: /etc/skills-gateway
              readOnly: true
            {{- end }}
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: http
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: http
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      volumes:
        {{- if ne .Values.persistence.mode "none" }}
        - name: data
          {{- include "skills-gateway.storageVolume" . | nindent 10 }}
        {{- else }}
        {{- include "skills-gateway.storageVolume" . }}
        {{- end }}
        - name: tmp
          emptyDir: {}
        {{- if .Values.config }}
        - name: config
          configMap:
            name: {{ include "skills-gateway.configMapName" . }}
        {{- end }}
