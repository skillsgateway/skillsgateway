# snapshot-vetting — delta for toggle-covers-external-connectors

`GW_VETTING_0029.1`'s text lives in `docs/reqstool/requirements.yml`, the SSOT;
the live entry here is the id-only pointer every requirement in this capability
takes. The delta therefore restates that pointer — see this change's
`design.md`, "Decision 4".

## MODIFIED Requirements

### Requirement: GW_VETTING_0029.1
The system SHALL implement GW_VETTING_0029.1.

#### Scenario: SVC_GW_VETTING_0029.1
The system SHALL pass SVC_GW_VETTING_0029.1.
